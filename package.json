{
  "name": "crm-platform",
  "scripts": {
    "frontend": "cd frontend && next dev",
    "backend": "cd backend && npx nest start --watch",
    "dev-front": "yarn frontend",
    "dev-back": "yarn backend",
    "git:front": "git add frontend/ && git commit -m",
    "git:back": "git add backend/ && git commit -m",
    "git:all": "git add . && git commit -m"
  },
  "dependencies": {
    "lucide-react": "^0.577.0",
    "recharts": "^3.8.0"
  }
}
